generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL") 
  //DATABASE URL comes from the .env file.
  //For MySQL databases the connection string looks like this: "mysql://user:password@localhost:3306/db_name"
}

model Question_Bank {
  id            Int       @id @default(autoincrement())
  ownerId       Int       // Foreign key linking to User
  title         String
  public        Boolean   // Whether the bank could be used by users other than the owner
  questions     Question[]
  rooms         Room[]
  user          User @relation(fields :[ownerId], references: [id])
}

model Question {
  id                Int       @id @default(autoincrement()) // Auto-incrementing ID
  questionBankId    Int       // Foreign key linking to Question_Banks
  text              String
  optionA           String
  optionB           String
  optionC           String
  optionD           String
  correctAnswer     Int       // Correct answer (1-4 -> have to be restricted outside of the scheme)
  bank              Question_Bank @relation(fields: [questionBankId], references: [id], onDelete: Cascade)
}

model Room {
  id                Int       @id
  hostId            String    @unique
  questionBankId    Int
  isOpen            Boolean
  bank              Question_Bank @relation(fields: [questionBankId], references: [id])
}

model User {
  id                Int    @id @default(autoincrement())
  username          String @unique
  password          String
  questionBanks     Question_Bank[]
}
